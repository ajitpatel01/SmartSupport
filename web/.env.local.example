# Public URL of the SmartSupport API (Express). Default matches local backend.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Set to true to use in-memory demo data (no API). See root README "Demo mode".
# NEXT_PUBLIC_DEMO_MODE=true
